@page "/categories"
@using BlazorBusiness.Repository.IRepository
@using ProjectModels
@inject IProductRepository _ProductRepository;
@inject NavigationManager _NavigationManager;

<a href="/categories/create" class="btn btn-outline-primary float-end"> Create Product</a>

<h3>Categories list</h3>
 

@if (_Categories == null)
{
    <div class="w-100 m-2">
        <div  class="w-100 text-center m-2">
            <p><b>Loading...</b></p>
            <div class="spinner-border text-primary" role="status">
                <span class="sr-only"> </span>
            </div>
        </div>
    </div>
}
else
{
    <table class="table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Product name</th>
            <th>Operations</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var product in _Categories)
        {
            <tr>
                <td>@product.Id</td>
                <td>@product.Name</td>
                <td>
                    <a href="/categories/@product.Id" class="btn btn-primary">Edit</a>
                    <button  class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#product-@product.Id">Delete</button>
                </td>
                 
                <!-- Modal -->
                <DeleteConfirm product="product" GetData="GetData"></DeleteConfirm>
            </tr>
        }
        </tbody>
    </table>
}


@code {
    public IEnumerable<ProductDTO> _Categories ;
    
    protected override async Task OnInitializedAsync()
    {
        await Task.Run(GetData);
    }

    protected void GetData()
    {
        _Categories = _ProductRepository.GetAll();
    }

}